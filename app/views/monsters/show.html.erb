<h1><%= @monster.name %></h1>
    <div class= "row">
        <div class= "col">
            <t><%= @monster.description %></t>
            <button class "btn btn-danger"> Eliminar </button>
        </div>
        <div class= "col">
            <% @monster.tweet.each do |tw| %>
                <t class= "Tweets"> <%= tw.content%> </t>
                <div>
            <%end%>
        </div>
    </div>
